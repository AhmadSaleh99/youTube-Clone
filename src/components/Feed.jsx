import { useState, useEffect } from "react";
import { Box, Stack, typography } from "@mui/material";

const Feed = () => {
  return <Stack sx={{ flexDirection: { sx: "column", md: "row" } }}></Stack>;
};

export default Feed;
